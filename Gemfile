# Gemfile
source "https://rubygems.org"

gem "jekyll", "~> 4.3.2"

# 添加其他必要的插件
group :jekyll_plugins do
  gem "jekyll-feed", "~> 0.16"
  gem "jekyll-seo-tag", "~> 2.8"
  # 其他插件...
end

# 确保使用 GitHub Pages 兼容的依赖版本
gem "github-pages", group: :jekyll_plugins
